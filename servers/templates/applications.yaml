{{- range .Values.applications }}
{{- $config := $.Values.config -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "aicoding-%s" .name | quote }}
  namespace: {{ $config.spec.namespace | quote }}
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ $config.spec.namespace | default .name | quote }}
    server: {{ $config.spec.destination.server | quote }}
  project: default
  source:
    path: {{ .path | default .name | quote }}
    repoURL: {{ $config.spec.source.repoURL }}
    targetRevision: {{ $config.spec.source.targetRevision }}
  ignoreDifferences:
    - group: "cae.cisco.com"
      kind: "RouteState"
  syncPolicy:
    syncOptions:
      - RespectIgnoreDifferences=true
    automated:
      prune: false
      selfHeal: true
---
{{ end -}}
